node_modules
dist
.output
.vinxi
.cache
.DS_Store
*.local
.env
.env.*
!.env.example
.vercel
.env*.local
src/data/sic-codes.json

# Turborepo
.turbo

# Local AI model files (CI caches these via actions/cache)
.litert-models

# Hand-labelling samples: company-to-URL pairs, i.e. the dataset itself
apps/web/.precision-samples

# Local operator notes — deliberately not published (the repo is public).
.claude/skills/

# Watch-mode log: local operator output, and it names live client fingerprints.
firewall-watch.log
