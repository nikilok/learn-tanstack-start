/**
 * Canonical hex colours for the light and dark theme backgrounds. Shared by
 * the pre-hydration theme-init script and the `<meta name="theme-color">`
 * tag so the mobile browser chrome matches first paint.
 */
export const THEME_COLORS = {
  light: '#ffffff',
  dark: '#0a0a0a',
} as const;
