{
  "regions": ["lhr1"],
  "outputDirectory": ".vercel/output",
  "installCommand": "bun install --frozen-lockfile",
  "ignoreCommand": "sh \"$(git rev-parse --show-toplevel)/apps/web/scripts/vercel-ignore.sh\""
}
